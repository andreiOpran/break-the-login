version: '3.8'

services:
  app:
    build: .
    ports:
      - "8082:8082"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./authx.db
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8082
